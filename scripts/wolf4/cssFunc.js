export let blackBackground = (element) => {
  element.classList.add('blackBackground');
};

export let yellowBorder = (element) => {
  element.classList.toggle('yellowBorder');
};
export let greenBorder = (element) => {
  element.classList.add('greenBorder');
};
export let pinkBackground = (element) => {
  element.classList.add('pinkBackground');
};
export let redText = (element) => {
  element.classList.add('redText');
};
export let borderRadius = (element) => {
  element.style.borderRadius = '5rem';
};

export let width = (element) => {
  element.classList.add('width');
};
